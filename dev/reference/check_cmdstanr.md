# Check that cmdstanr is installed

Checks that the `cmdstanr` package is available and provides a helpful
error message if not.

## Usage

``` r
check_cmdstanr()
```

## Value

NULL (called for side effect of raising an error if cmdstanr is not
installed)
