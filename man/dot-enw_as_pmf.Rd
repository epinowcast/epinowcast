% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-module-helpers.R
\name{.enw_as_pmf}
\alias{.enw_as_pmf}
\title{Convert a distribution to a probability mass function}
\usage{
.enw_as_pmf(x, arg = "x", drop_zero = FALSE)
}
\arguments{
\item{x}{A numeric vector describing a PMF or a \verb{<dist_spec>}.}

\item{arg}{A character string naming the argument being converted (used
in error messages).}

\item{drop_zero}{Logical, defaults to \code{FALSE}. If \code{TRUE}, the probability
of a delay of zero days is dropped from a discretised \verb{<dist_spec>} and
the PMF renormalised so that the first entry is the probability of a
delay of one day (as required for a generation time).}
}
\value{
A numeric vector describing a PMF.
}
\description{
Numeric vectors are returned unchanged. A \verb{<dist_spec>} from the
\code{distspec} package (for example \code{distspec::Gamma(mean = 4, sd = 3, max = 15)}) is discretised to a daily probability mass function (PMF)
with \code{\link[distspec:discretise]{distspec::discretise()}}, with the first entry the probability of a
delay of zero days. The distribution must be bounded (using \code{max} or
\code{cdf_max}) and have fixed (numeric) parameters. Sums of distributions
are convolved with \code{\link[distspec:collapse]{distspec::collapse()}}.
}
\keyword{internal}
