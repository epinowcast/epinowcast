% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-module-helpers.R
\name{enw_uncertain}
\alias{enw_uncertain}
\title{Specify an uncertain parametric delay distribution}
\usage{
enw_uncertain(
  distribution = c("lognormal", "exponential", "gamma", "loglogistic"),
  mean = c(1, 1),
  sd = c(0.5, 1),
  max
)
}
\arguments{
\item{distribution}{A character string giving the parametric family. One of
\code{"lognormal"} (the default), \code{"exponential"}, \code{"gamma"}, or \code{"loglogistic"}.
These match the families supported by \code{\link[=enw_reference]{enw_reference()}}.}

\item{mean}{A length-2 numeric vector \code{c(mean, sd)} giving the mean and
standard deviation of the \code{Normal} prior on the distribution's location
parameter (\code{mu}). For the lognormal family this is the prior on the
\code{meanlog}. Defaults to a weakly informative \code{c(1, 1)}.}

\item{sd}{A length-2 numeric vector \code{c(mean, sd)} giving the mean and
standard deviation of the zero-truncated \code{Normal} prior on the
distribution's scale parameter (\code{sigma}). For the lognormal family this is
the prior on the \code{sdlog}. Ignored for the single-parameter exponential
family. Defaults to \code{c(0.5, 1)}.}

\item{max}{Integer maximum delay (in timesteps) at which the distribution is
truncated and discretised. Must be at least 2. The resulting PMF has \code{max}
entries (delays \code{0} to \code{max - 1}).}
}
\value{
A list of class \code{enw_uncertain} describing the uncertain delay
distribution, with elements \code{distribution}, \code{dist_id} (the Stan integer
code), \code{max}, \code{mean_p}, and \code{sd_p}.
}
\description{
Builds a specification for a delay distribution whose parameters are
uncertain and estimated within the model rather than supplied as a fixed
probability mass function (PMF). The distribution is discretised in-model
using the same machinery as the parametric reference date model
(see \code{\link[=enw_reference]{enw_reference()}}) so that a fresh PMF is rebuilt for each posterior
sample. This provides parity with the uncertain distribution support in
\code{EpiNow2} (which draws delay parameters from priors and marginalises them
within the model).

Uncertain distributions can be passed to the \code{generation_time} and
\code{latent_reporting_delay} arguments of \code{\link[=enw_expectation]{enw_expectation()}} in place of a
fixed numeric PMF.
}
\examples{
# An uncertain lognormal generation time
enw_uncertain(
  distribution = "lognormal",
  mean = c(1.6, 0.05), sd = c(0.4, 0.05), max = 15
)
}
\seealso{
Helper functions for model modules
\code{\link{add_max_observed_delay}()},
\code{\link{add_pmfs}()},
\code{\link{convolution_matrix}()},
\code{\link{enw_dayofweek_structural_reporting}()},
\code{\link{enw_reference_by_report}()},
\code{\link{enw_reps_with_complete_refs}()},
\code{\link{enw_structural_reporting_metadata}()},
\code{\link{extract_obs_metadata}()},
\code{\link{extract_sparse_matrix}()},
\code{\link{latest_obs_as_matrix}()}
}
\concept{modelmodulehelpers}
