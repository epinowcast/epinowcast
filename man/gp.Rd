% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula-tools.R
\name{gp}
\alias{gp}
\title{Adds an approximate Gaussian process to the model.}
\usage{
gp(
  time,
  by,
  d = 0,
  kernel = c("matern32", "matern52", "ou", "se", "periodic"),
  basis_prop = 0.2,
  boundary_scale = 1.5
)
}
\arguments{
\item{time}{Defines the time index of the Gaussian process. Must be
numeric.}

\item{by}{Optional grouping variable. If supplied, an independent
Gaussian process is fitted for each level of \code{by} (sharing the
length scale and magnitude hyperparameters). Currently limited to a
single variable.}

\item{d}{Non-negative integer, defaults to \code{0}. Order of
differencing, matching the \code{d} of \code{\link[=arima]{arima()}}: the per-group
realisation is integrated (cumulative-summed) \code{d} times before it is
added to the predictor. \code{d = 0} gives stationary deviations (the
default, equivalent to EpiNow2's \code{gp_on = "R0"}). \code{d = 1} integrates
once, giving a smoothly drifting, random-walk-like trajectory
(equivalent to EpiNow2's default \code{gp_on = "R_t-1"}). For \code{d >= 1} the
first \code{d} values of the realisation are anchored to zero, so the free
level (and, for \code{d >= 2}, slope) is carried by the module's fixed
effects rather than the GP. Differencing is intended for the latent
expectation modules (the growth rate \code{expr} and latent-to-obs
proportion \code{expl}); integrating a logit-hazard term (\code{refnp}, \code{rep},
\code{miss}) is unusual but permitted for API consistency with \code{\link[=arima]{arima()}}.}

\item{kernel}{Character string selecting the covariance kernel. One
of \code{"matern32"} (the default, a Matern 3/2 kernel), \code{"matern52"}
(Matern 5/2), \code{"ou"} (Ornstein-Uhlenbeck, equivalent to Matern
1/2), \code{"se"} (squared exponential), or \code{"periodic"}.}

\item{basis_prop}{Numeric in \verb{(0, 1]}. Proportion of time points to
use as basis functions, controlling the accuracy-speed trade-off of
the reduced-rank approximation. Defaults to \code{0.2} (the \code{EpiNow2}
default). The number of basis functions is
\code{ceiling(basis_prop * T)}.}

\item{boundary_scale}{Numeric, defaults to \code{1.5}. Boundary factor
\code{L} of the Hilbert-space approximation; the process is approximated
on the interval scaled by this factor. This has no effect when
\code{kernel = "periodic"}, which uses a fundamental-frequency basis
rather than the boundary-scaled basis.}
}
\value{
A list of class \code{enw_gp_term} describing the Gaussian
process term, interpretable by \code{\link[=construct_gp]{construct_gp()}}.
}
\description{
A call to \code{gp()} can be used in the \code{formula} argument of
model construction functions in the \code{epinowcast} package such as
\code{\link[=enw_formula]{enw_formula()}}. It declares a Hilbert-space reduced-rank
(spectral) approximate Gaussian process indexed by \code{time} (and
optionally a grouping variable \code{by}) whose value at each observation
is added to the linear predictor. As with \code{\link[=arima]{arima()}}, arguments are
not evaluated; they are passed by name for use in model construction.

Like \code{\link[=arima]{arima()}} and \code{\link[=rw]{rw()}}, a \code{gp()} term works on every module that
takes a formula, each with its own prior prefix:
\itemize{
\item \code{\link[=enw_expectation]{enw_expectation()}} — the growth rate (\code{expr}) and the latent-to-obs
proportion (\code{expl}).
\item \code{\link[=enw_reference]{enw_reference()}} — the parametric delay mean (\code{refp}) and the
non-parametric logit hazards (\code{refnp}).
\item \code{\link[=enw_report]{enw_report()}} — report-date logit hazards (\code{rep}).
\item \code{\link[=enw_missing]{enw_missing()}} — the missing-reference proportion (\code{miss}).
}

At most one \code{gp()} term is currently supported per formula (the
multiple-term example shown for \code{\link[=gp_terms]{gp_terms()}} only illustrates term
detection, not a supported model). The default \code{alpha} (magnitude)
and length-scale priors are inherited from \code{EpiNow2} and are set on
\code{EpiNow2}'s scale; on a given module's scale (for example the log
growth rate or a logit hazard) they may need tuning with
\code{\link[=enw_replace_priors]{enw_replace_priors()}}.
}
\section{Reference}{

The Stan implementation of the approximate Gaussian process is
adapted from \code{EpiNow2} (https://github.com/epiforecasts/EpiNow2,
MIT licensed). The Hilbert-space approximation follows
Riutort-Mayol et al. (2023), doi:10.1007/s11222-022-10167-2.
}

\examples{
gp(time)
gp(time, location)
gp(time, kernel = "se", basis_prop = 0.3)
gp(time, d = 1)
}
\seealso{
Functions used to help convert formulas into model designs
\code{\link{ar}()},
\code{\link{arima}()},
\code{\link{arima_terms}()},
\code{\link{arma}()},
\code{\link{as_string_formula}()},
\code{\link{construct_arima}()},
\code{\link{construct_gp}()},
\code{\link{construct_re}()},
\code{\link{construct_rw}()},
\code{\link{enw_formula}()},
\code{\link{enw_manual_formula}()},
\code{\link{gp_terms}()},
\code{\link{ma}()},
\code{\link{parse_formula}()},
\code{\link{re}()},
\code{\link{remove_arima_terms}()},
\code{\link{remove_gp_terms}()},
\code{\link{remove_rw_terms}()},
\code{\link{rw}()},
\code{\link{rw_terms}()},
\code{\link{split_formula_to_terms}()}
}
\concept{formulatools}
