% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-module-helpers.R
\name{delay_only_ltotal}
\alias{delay_only_ltotal}
\title{Known per-reference-date totals for the delay-only model}
\usage{
delay_only_ltotal(data, delay_only)
}
\arguments{
\item{data}{Output from \code{\link[=enw_preprocess_data]{enw_preprocess_data()}}.}

\item{delay_only}{Logical; if \code{FALSE} an empty (\code{g} by \code{0}) matrix is
returned so the model carries no delay-only totals.}
}
\value{
A \code{g} by \code{t} matrix of log totals (or \code{g} by \code{0} when not in
delay-only mode).
}
\description{
Builds the \code{dlo_ltotal} data entry for the delay-only model. The known
totals are the latest available \code{confirm} per reference date and group,
supplied to Stan on the log scale as a \code{g} by \code{t} matrix (cmdstanr's layout
for \verb{array[g] vector[t]}). The log total is only an offset on the expected
cells and cancels in the multinomial likelihood, so reference dates with no
observed cells (a non-positive or missing latest total, e.g. the most recent
dates under an observation indicator) are floored to 1 (log 0); such dates
contribute nothing to the likelihood.
}
\seealso{
Helper functions for model modules
\code{\link{add_max_observed_delay}()},
\code{\link{add_pmfs}()},
\code{\link{convolution_matrix}()},
\code{\link{enw_dayofweek_structural_reporting}()},
\code{\link{enw_reference_by_report}()},
\code{\link{enw_reps_with_complete_refs}()},
\code{\link{enw_structural_reporting_metadata}()},
\code{\link{extract_obs_metadata}()},
\code{\link{extract_sparse_matrix}()},
\code{\link{latest_obs_as_matrix}()}
}
\concept{modelmodulehelpers}
