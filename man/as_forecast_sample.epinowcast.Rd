% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-validation.R
\name{as_forecast_sample.epinowcast}
\alias{as_forecast_sample.epinowcast}
\title{Convert an epinowcast object to a forecast_sample object}
\usage{
\method{as_forecast_sample}{epinowcast}(data, latest_obs, ...)
}
\arguments{
\item{data}{An \code{epinowcast} nowcast object as returned by
\code{\link[=epinowcast]{epinowcast()}}.}

\item{latest_obs}{Latest observations to use for the true values must
contain \code{confirm} and \code{observed} variables.}

\item{...}{Additional arguments passed to
\code{\link[scoringutils:as_forecast_sample]{scoringutils::as_forecast_sample()}}}
}
\value{
A \code{forecast_sample} object as returned by
\code{\link[scoringutils:as_forecast_sample]{scoringutils::as_forecast_sample()}}
}
\description{
This function is used to convert an \code{epinowcast} as returned by
\code{\link[=epinowcast]{epinowcast()}} object to a \code{forecast_sample} object which can be used for
scoring using the \code{scoringutils} package.
}
\details{
This function performs date alignment validation before merging nowcast
samples with latest observations. It checks that the latest observation
date exists in the nowcast dates, and provides informative error messages
if misalignment is detected. The check automatically detects the timestep
(daily, weekly, monthly) and includes relevant information in error
messages (e.g., weekday information for weekly data). This validation
helps prevent silent failures when nowcast and observation data use
different date conventions, which can occur particularly with weekly or
monthly aggregated data.
}
\examples{
\dontshow{if (interactive()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
library(scoringutils)

nowcast <- enw_example("nowcast")
latest_obs <- enw_example("observations")
as_forecast_sample(nowcast, latest_obs)
\dontshow{\}) # examplesIf}
}
\seealso{
Other modelvalidation: 
\code{\link{enw_score_nowcast}()}
}
\concept{modelvalidation}
