% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/laplace.R
\name{enw_laplace_marginal_inits}
\alias{enw_laplace_marginal_inits}
\title{Initial values for the embedded-Laplace nowcast model}
\usage{
enw_laplace_marginal_inits(data)
}
\arguments{
\item{data}{A data list as produced by \code{\link[=enw_laplace_marginal_data]{enw_laplace_marginal_data()}}.}
}
\value{
A function with no arguments returning a named list of initial
values suitable for the \code{init} argument of a \code{cmdstanr} \verb{$sample()} call.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Builds a randomised initialisation function for the embedded-Laplace model
that starts the level intercept near the scale of the observed counts and
the delay and overdispersion parameters near their priors. This avoids the
non-finite log-likelihood rejections that occur when the default wide
initialisation places the linear predictor far from the data.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
pobs <- enw_example("preprocessed")
dl <- enw_laplace_marginal_data(pobs)
init_fn <- enw_laplace_marginal_inits(dl)
init_fn()
\dontshow{\}) # examplesIf}
}
\seealso{
Other laplace: 
\code{\link{check_laplace_supported}()},
\code{\link{enw_laplace_marginal}()},
\code{\link{enw_laplace_marginal_data}()},
\code{\link{split_laplace_design}()}
}
\concept{laplace}
