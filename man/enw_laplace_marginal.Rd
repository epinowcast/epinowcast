% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/laplace.R
\name{enw_laplace_marginal}
\alias{enw_laplace_marginal}
\title{Nowcast using the experimental embedded-Laplace path}
\usage{
enw_laplace_marginal(
  data,
  expectation = epinowcast::enw_expectation(r = ~1, data = data),
  reference = epinowcast::enw_reference(parametric = ~1, distribution = "lognormal", data
    = data),
  obs = epinowcast::enw_obs(family = "negbin", data = data),
  report = epinowcast::enw_report(data = data),
  missing = epinowcast::enw_missing(formula = ~0, data = data),
  fit = epinowcast::enw_fit_opts(sampler = epinowcast::enw_sample, nowcast = TRUE, pp =
    FALSE, likelihood = TRUE),
  model = epinowcast::enw_laplace_marginal_model(),
  priors = NULL,
  jitter = 1e-06
)
}
\arguments{
\item{data}{A preprocessed observation object from \code{\link[=enw_preprocess_data]{enw_preprocess_data()}}.}

\item{expectation}{An expectation module from \code{\link[=enw_expectation]{enw_expectation()}} supplying
the linear predictor design via its \code{r} formula. The formula is re-fit on
the reference-date metadata to extract the fixed and random effect designs.}

\item{reference}{A reference module from \code{\link[=enw_reference]{enw_reference()}} supplying the
static parametric delay distribution.}

\item{obs}{An observation module from \code{\link[=enw_obs]{enw_obs()}} supplying the observation
family and reporting triangle.}

\item{report}{A report module from \code{\link[=enw_report]{enw_report()}}. Only the default
(no report-date model) is supported.}

\item{missing}{A missing module from \code{\link[=enw_missing]{enw_missing()}}. Only the default
(no missing-reference model) is supported.}

\item{fit}{Model fit options as defined using \code{\link[=enw_fit_opts]{enw_fit_opts()}}.}

\item{model}{A compiled embedded-Laplace \code{cmdstanr} model as returned by
\code{\link[=enw_laplace_marginal_model]{enw_laplace_marginal_model()}}.}

\item{priors}{A \code{data.frame} with columns \code{variable}, \code{mean} and \code{sd}
describing normal priors that replace the module defaults.}

\item{jitter}{Numeric diagonal jitter added to the covariance for numerical
stability. Defaults to \code{1e-6}.}
}
\value{
An object of class "epinowcast" combining the input data, priors and
the embedded-Laplace fit.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

An opt-in, experimental alternative to \code{\link[=epinowcast]{epinowcast()}} that marginalises the
latent log-expected-count field analytically using Stan's embedded Laplace
approximation (\verb{laplace_marginal_*}, cmdstan >= 2.39) instead of sampling it
with NUTS. The hyperparameters (fixed effects, random-effect standard
deviations, Gaussian process magnitude and length scale, delay parameters
and overdispersion) are still sampled with NUTS; only the high-dimensional
latent field is integrated out.

This path supports a subset of the full model: a linear expectation with
fixed effects, random effects and/or a single \code{gp()} term; a static
parametric reference delay (lognormal, gamma or exponential); and a negative
binomial (NB2) or Poisson observation family. The renewal expectation,
non-parametric reference hazards, report-date models, missing-reference
models and time-varying delays are not supported and raise a clear error.

The returned object has the same structure as an \code{\link[=epinowcast]{epinowcast()}} fit (a
\code{data.table} carrying \code{fit}, \code{data}, \code{fit_args} and \code{run_time}) joined to
the preprocessed data, so \code{\link[=summary.epinowcast]{summary.epinowcast()}} and the nowcast extractors
work as usual.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
pobs <- enw_example("preprocessed")
nowcast <- enw_laplace_marginal(
  pobs,
  expectation = enw_expectation(~ 1 + (1 | day_of_week), data = pobs),
  fit = enw_fit_opts(
    chains = 2, iter_warmup = 500, iter_sampling = 500
  )
)
summary(nowcast, type = "nowcast")
\dontshow{\}) # examplesIf}
}
\seealso{
Other laplace: 
\code{\link{check_laplace_supported}()},
\code{\link{enw_laplace_marginal_data}()},
\code{\link{enw_laplace_marginal_inits}()},
\code{\link{split_laplace_design}()}
}
\concept{laplace}
