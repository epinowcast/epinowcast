% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast.R
\name{enw_forecast_horizon}
\alias{enw_forecast_horizon}
\title{Forecast a fitted nowcast beyond the fitted window}
\usage{
enw_forecast_horizon(fit, overrides, horizon, model, ...)
}
\arguments{
\item{fit}{A fitted \code{\link[=epinowcast]{epinowcast()}} object.}

\item{overrides}{A named list of model-component overrides (see Details).
The empty list (the default) re-drives the fit using the per-draw posterior.}

\item{horizon}{Integer number of future time steps (already validated).}

\item{model}{The compiled model to use, as returned by \code{\link[=enw_model]{enw_model()}}.}

\item{...}{Additional arguments passed to the \code{generate_quantities} method
of the \code{cmdstanr} model.}
}
\value{
An object of class \code{epinowcast}.
}
\description{
Internal worker for \code{\link[=enw_forecast]{enw_forecast()}} when \code{horizon > 0}. Extends the
modelled reference dates forward, then re-runs the generated quantities per
posterior draw so each draw keeps its own fitted history while the future
window is driven by the supplied growth rate.
}
\keyword{internal}
