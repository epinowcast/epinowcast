% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula-tools.R
\name{arima}
\alias{arima}
\title{Adds an ARIMA(p, d, q) latent residual to the model.}
\usage{
arima(time, by, p = 1, d = 0, q = 0)
}
\arguments{
\item{time}{Defines the time index of the ARIMA process.}

\item{by}{Optional grouping variable. If supplied, an independent
ARIMA series is fitted for each level of \code{by}. Currently limited to
a single variable.}

\item{p}{Non-negative integer. Order of the autoregressive part.
Defaults to 1.}

\item{d}{Non-negative integer. Order of differencing (\code{d = 1} gives
an integrated series, equivalent to \code{rw()} when \code{p = q = 0}).
Defaults to 0.}

\item{q}{Non-negative integer. Order of the moving-average part.
Defaults to 0.}
}
\value{
A list of class \code{enw_arima_term} describing the ARIMA term,
interpretable by \code{\link[=construct_arima]{construct_arima()}}. Each group draws an independent
shock series; \code{phi}, \code{theta}, and \code{sigma} are shared across groups
(per-group parameters are a planned extension).
}
\description{
A call to \code{arima()} can be used in the \code{formula} argument
of model construction functions in the \code{epinowcast} package such as
\code{\link[=enw_formula]{enw_formula()}}. It declares an ARIMA(p, d, q) latent series indexed
by \code{time} (and optionally a grouping variable \code{by}) whose value at
each observation is added to the linear predictor. As with \code{\link[=rw]{rw()}},
arguments are not evaluated; they are passed by name for use in
model construction. Setting \code{p = d = q = 0} is not allowed; use
\code{\link[=rw]{rw()}} (equivalent to \code{arima(time, d = 1)}) for a random walk.
}
\examples{
arima(time)
arima(time, location)
arima(time, location, p = 2, d = 1, q = 1)
}
\seealso{
Functions used to help convert formulas into model designs
\code{\link{arima_terms}()},
\code{\link{arma_aliases}},
\code{\link{as_string_formula}()},
\code{\link{construct_arima}()},
\code{\link{construct_re}()},
\code{\link{construct_rw}()},
\code{\link{enw_formula}()},
\code{\link{enw_manual_formula}()},
\code{\link{parse_formula}()},
\code{\link{re}()},
\code{\link{remove_arima_terms}()},
\code{\link{remove_rw_terms}()},
\code{\link{rw}()},
\code{\link{rw_terms}()},
\code{\link{split_formula_to_terms}()}
}
\concept{formulatools}
