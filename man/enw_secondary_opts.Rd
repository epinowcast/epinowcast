% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-modules.R
\name{enw_secondary_opts}
\alias{enw_secondary_opts}
\title{Secondary observation target options}
\usage{
enw_secondary_opts(
  type = c("incidence", "prevalence"),
  cumulative = NULL,
  historic = NULL,
  primary_hist_additive = NULL,
  current = NULL,
  primary_current_additive = NULL
)
}
\arguments{
\item{type}{Character string, one of \code{"incidence"} (the default) or
\code{"prevalence"}. Sets sensible defaults for the binary switches below.}

\item{cumulative}{Optional integer (\code{0}/\code{1}) override for the cumulative
switch. When \code{NULL} (the default) the value implied by \code{type} is used.}

\item{historic}{Optional integer (\code{0}/\code{1}) override for the historic switch.}

\item{primary_hist_additive}{Optional integer (\code{0}/\code{1}) override controlling
whether the historic primary term is additive.}

\item{current}{Optional integer (\code{0}/\code{1}) override for the current switch.}

\item{primary_current_additive}{Optional integer (\code{0}/\code{1}) override
controlling whether the current primary term is additive.}
}
\value{
A named list of integer switches (prefixed with \code{sec_}) for use by
\code{\link[=enw_secondary]{enw_secondary()}}.
}
\description{
Defines how a secondary observation relates to the primary
series, mirroring the parameterisation used by \code{EpiNow2::secondary_opts()}
(MIT licensed, see Note). The two built-in \code{type}s cover the common cases:
an \emph{incidence} secondary outcome (a delayed, scaled convolution of the
primary series, e.g. cases to deaths) and a \emph{prevalence} secondary outcome
(a cumulative quantity such as bed occupancy that accumulates inflow and
loses outflow over time).

The relationship is built from five binary switches (each \code{0} or \code{1}):
\itemize{
\item \code{cumulative}: carry the previous secondary value forward (prevalence).
\item \code{historic}: include the convolution of past primary observations.
\item \code{primary_hist_additive}: add (\code{1}) or subtract (\code{0}) the historic term.
\item \code{current}: include the (scaled) current primary observation.
\item \code{primary_current_additive}: add (\code{1}) or subtract (\code{0}) the current term.
}
}
\note{
The switch parameterisation and the corresponding Stan
\code{calculate_secondary()} function are adapted from \code{EpiNow2}
(\url{https://github.com/epiforecasts/EpiNow2}, MIT licensed).
}
\examples{
# Incidence secondary outcome (e.g. cases -> deaths)
enw_secondary_opts()

# Prevalence secondary outcome (e.g. bed occupancy)
enw_secondary_opts("prevalence")
}
\seealso{
Model modules
\code{\link{enw_expectation}()},
\code{\link{enw_fit_opts}()},
\code{\link{enw_missing}()},
\code{\link{enw_obs}()},
\code{\link{enw_reference}()},
\code{\link{enw_report}()},
\code{\link{enw_secondary}()}
}
\concept{modelmodules}
