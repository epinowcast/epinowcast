% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postprocess.R
\name{enw_posterior_delay}
\alias{enw_posterior_delay}
\title{Posterior samples of the parametric reporting-delay distribution}
\usage{
enw_posterior_delay(fit, max_delay, distribution = "lognormal", draws = NULL)
}
\arguments{
\item{fit}{A \code{cmdstanr} fit object (the \code{fit[[1]]} element of an
\code{epinowcast} output).}

\item{max_delay}{Maximum delay (number of delay slots, delays
\code{0:(max_delay - 1)}).}

\item{distribution}{The parametric distribution used in \code{\link[=enw_reference]{enw_reference()}},
one of "lognormal", "gamma", "exponential", or "loglogistic". Defaults to
"lognormal". A \code{cmdstanr} fit does not record which distribution was used
(the \code{model_refp} id is Stan data, not saved to the draws), so this must
match the distribution passed to \code{\link[=enw_reference]{enw_reference()}}. An incorrect value
silently yields the wrong PMF.}

\item{draws}{Optional integer; if supplied, a random subset of this many
posterior draws is returned (useful for plotting). The same draws are used
across all reference-design rows.}
}
\value{
A long \code{data.table} with columns \code{.draw}, \code{delay}, and \code{pmf}, plus
an integer \code{row} column when the delay model has more than one
reference-design row.
}
\description{
Extracts posterior draws of the parametric reporting-delay distribution
from a fit (e.g. a delay-only fit, see the delay estimation vignette) and
returns the discretised delay probability mass function for each draw. This
supports comparing the estimated distribution against a known truth and
plotting posterior samples rather than only summaries.
}
\details{
The function reads the full \code{refp_mean} and \code{refp_sd} vectors saved by the
Stan model (one entry per unique combination of parametric reference-date
covariates) and discretises a delay PMF for each. For an intercept-only
delay (the common case, \code{refp_mean} of length one) the output is a long
\code{data.table} with columns \code{.draw}, \code{delay}, and \code{pmf}. For a delay model
with reference-date covariates, random effects, or time- or group-varying
delays (\code{refp_mean} of length greater than one) an additional integer \code{row}
column identifies the reference-design row, and each row may have a
different PMF. The mapping from \code{row} to reference date and group is set by
the reference module's fixed-effect design (see \code{\link[=enw_reference]{enw_reference()}} and
\code{enw_formula_as_data_list()}'s \code{refp_findex}); it is not recovered here
because a \code{cmdstanr} fit does not retain that design on its own.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
fit <- enw_example("nowcast")
# Intercept-only delay
enw_posterior_delay(fit$fit[[1]], max_delay = 20, draws = 50)

# A delay model with a reference-date covariate yields one PMF per
# reference-design row, identified by the `row` column:
\dontrun{
pobs <- enw_example("preprocessed")
covariate_fit <- epinowcast(
  pobs,
  reference = enw_reference(~ 1 + day_of_week, data = pobs),
  obs = enw_obs(delay_only = TRUE, data = pobs)
)
enw_posterior_delay(covariate_fit$fit[[1]], max_delay = 20, draws = 50)
}
\dontshow{\}) # examplesIf}
}
\seealso{
Functions used for postprocessing of model fits
\code{\link{.check_primarycensored}()},
\code{\link{.delay_draw_columns}()},
\code{\link{.discretise_parametric_pmf}()},
\code{\link{build_ord_obs}()},
\code{\link{enw_add_latest_obs_to_nowcast}()},
\code{\link{enw_nowcast_samples}()},
\code{\link{enw_nowcast_summary}()},
\code{\link{enw_posterior}()},
\code{\link{enw_pp_summary}()},
\code{\link{enw_quantiles_to_long}()},
\code{\link{enw_summarise_samples}()},
\code{\link{subset_obs}()}
}
\concept{postprocess}
