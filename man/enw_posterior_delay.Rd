% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postprocess.R
\name{enw_posterior_delay}
\alias{enw_posterior_delay}
\title{Posterior samples of the parametric reporting-delay distribution}
\usage{
enw_posterior_delay(fit, max_delay, distribution = "lognormal", draws = NULL)
}
\arguments{
\item{fit}{A \code{cmdstanr} fit object (the \code{fit[[1]]} element of an
\code{epinowcast} output).}

\item{max_delay}{Maximum delay (number of delay slots). Defaults to the
length of the delay PMF implied by the fit's \code{refp_mean} if available,
otherwise must be supplied.}

\item{distribution}{The parametric distribution used in \code{\link[=enw_reference]{enw_reference()}}.
Defaults to "lognormal".}

\item{draws}{Optional integer; if supplied, a random subset of this many
posterior draws is returned (useful for plotting).}
}
\value{
A long \code{data.table} with columns \code{.draw}, \code{delay}, and \code{pmf}.
}
\description{
Extracts posterior draws of the parametric reporting-delay distribution
from a fit (e.g. a delay-only fit, see the delay estimation vignette) and
returns the discretised delay probability mass function for each draw. This
supports comparing the estimated distribution against a known truth and
plotting posterior samples rather than only summaries.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
fit <- enw_example("nowcast")
enw_posterior_delay(fit$fit[[1]], max_delay = 20, draws = 50)
\dontshow{\}) # examplesIf}
}
\seealso{
Functions used for postprocessing of model fits
\code{\link{.check_primarycensored}()},
\code{\link{.discretise_parametric_pmf}()},
\code{\link{build_ord_obs}()},
\code{\link{enw_add_latest_obs_to_nowcast}()},
\code{\link{enw_nowcast_samples}()},
\code{\link{enw_nowcast_summary}()},
\code{\link{enw_posterior}()},
\code{\link{enw_pp_summary}()},
\code{\link{enw_quantiles_to_long}()},
\code{\link{enw_summarise_samples}()},
\code{\link{subset_obs}()}
}
\concept{postprocess}
