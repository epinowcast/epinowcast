% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula-tools.R
\name{secondary}
\alias{secondary}
\title{Declare a stratum as a delayed function of another stratum}
\usage{
secondary(
  parent,
  delay = ~1,
  distribution = "lognormal",
  report = ~0,
  ascertainment = ~1,
  type = c("incidence", "prevalence")
)
}
\arguments{
\item{parent}{The parent stratum that this stratum depends on. Either
an unquoted name (e.g. \code{cases}) or a string (e.g. \code{"cases"}). Stored
as a string.}

\item{delay}{A formula describing the cross-stratum delay (reference)
surface. Defaults to \code{~1}. Maps to the reference hazard machinery.}

\item{distribution}{Character string giving the parametric family of
the cross-stratum delay. One of \code{"lognormal"} (the default),
\code{"gamma"}, or \code{"exponential"}. This is the only non-formula argument.}

\item{report}{A formula describing the report-date hazard surface for
the cross-stratum delay. Defaults to \code{~0} (no report-date effects).}

\item{ascertainment}{A formula describing the ascertainment (scale)
surface, for example a case fatality ratio. Defaults to \code{~1}. Maps to
the latent-to-observation proportion machinery.}

\item{type}{Character string, one of \code{"incidence"} (the default) or
\code{"prevalence"}, selecting whether the dependent stratum is modelled as
an incidence or prevalence function of the parent.}
}
\value{
A list of class \code{enw_secondary_term} describing the
dependency, interpretable by \code{\link[=construct_secondary]{construct_secondary()}}. It contains the
\code{parent} (string), the \code{delay}, \code{report}, and \code{ascertainment}
formulas, the delay \code{distribution}, and the \code{type}.
}
\description{
A call to \code{secondary()} is used inside a per-stratum
expectation formula passed to \code{\link[=enw_expectation]{enw_expectation()}} (the \code{r} argument as
a named list). It declares that the stratum carrying the formula is a
delayed, ascertained (scaled) function of a parent stratum's
expectation rather than having its own latent process. For example a
deaths stratum may be a delayed and scaled function of a cases
stratum. As with \code{\link[=gp]{gp()}} and \code{\link[=arima]{arima()}}, arguments are not evaluated;
they are captured for use in model construction.

Every effect surface is a formula and follows the \code{\link[=enw_formula]{enw_formula()}}
syntax. The only non-formula argument is \code{distribution}, the
parametric family of the cross-stratum delay. Chains are allowed (a
stratum may depend on a non-primary stratum); the dependency graph is
validated and topologically ordered by \code{\link[=enw_topo_sort_strata]{enw_topo_sort_strata()}}.

This is the Phase 0 R skeleton of the multi-stratum overlay: the term
is parsed, validated, and ordered, but is not yet wired into the Stan
model.
}
\examples{
secondary(cases)
secondary("cases", delay = ~ 1 + week, distribution = "gamma")
secondary(cases, ascertainment = ~ 1 + (1 | region), type = "prevalence")
}
\seealso{
Functions used to help convert formulas into model designs
\code{\link{ar}()},
\code{\link{arima}()},
\code{\link{arima_terms}()},
\code{\link{arma}()},
\code{\link{as_string_formula}()},
\code{\link{construct_arima}()},
\code{\link{construct_gp}()},
\code{\link{construct_re}()},
\code{\link{construct_rw}()},
\code{\link{construct_secondary}()},
\code{\link{enw_formula}()},
\code{\link{enw_manual_formula}()},
\code{\link{gp}()},
\code{\link{gp_terms}()},
\code{\link{ma}()},
\code{\link{parse_formula}()},
\code{\link{re}()},
\code{\link{remove_arima_terms}()},
\code{\link{remove_gp_terms}()},
\code{\link{remove_rw_terms}()},
\code{\link{remove_secondary_terms}()},
\code{\link{rw}()},
\code{\link{rw_terms}()},
\code{\link{secondary_terms}()},
\code{\link{split_formula_to_terms}()}
}
\concept{formulatools}
