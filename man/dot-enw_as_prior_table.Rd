% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-tools.R
\name{.enw_as_prior_table}
\alias{.enw_as_prior_table}
\title{Coerce prior specifications to a prior table}
\usage{
.enw_as_prior_table(x, template = NULL, arg = "priors")
}
\arguments{
\item{x}{A named list of \verb{<dist_spec>} objects, a \code{data.frame} with a
\code{variable} column and a \code{prior} list column, or a \code{data.frame} with
\code{variable}, \code{mean}, and \code{sd} columns.}

\item{template}{An optional prior table (with \code{variable} and
\code{distribution} columns) used to look up the prior family for entries of
\code{x} given as a mean and standard deviation.}

\item{arg}{A character string naming the argument being coerced (used in
error messages).}
}
\value{
A \code{data.table} with a \code{variable} column and a \code{prior} list
column. Other columns of a \code{data.frame} input are retained.
}
\description{
Converts the supported ways of specifying priors into a \code{data.table}
with a \code{variable} column and a \code{prior} list column of \verb{<dist_spec>}
objects. See \code{\link[=enw_replace_priors]{enw_replace_priors()}} for the supported formats.
}
\keyword{internal}
