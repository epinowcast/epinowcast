% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-tools.R
\name{.enw_prior_table}
\alias{.enw_prior_table}
\title{Build a module prior table}
\usage{
.enw_prior_table(variable, description, distribution, prior, dimension = NULL)
}
\arguments{
\item{variable}{Character vector of prior variable names (without the
\verb{_p} suffix used in the Stan model).}

\item{description}{Character vector describing each prior.}

\item{distribution}{Character vector naming the prior family the Stan
model applies to each variable. One of \code{"Normal"},
\code{"Zero truncated normal"}, \code{"Log normal"}, or \code{"Uniform"}.}

\item{prior}{A list of \verb{<dist_spec>} objects with the same length as
\code{variable}, giving the parameters of each prior. Use \code{NULL} for a flat
prior (only supported for \code{"Uniform"} entries).}

\item{dimension}{Optional integer vector giving the index of each entry
of a vectorised prior. Defaults to \code{NULL} (no \code{dimension} column).}
}
\value{
A \code{data.table} with columns \code{variable}, \code{dimension} (when
supplied), \code{description}, \code{distribution}, and \code{prior}.
}
\description{
Internal constructor for the \code{data.table} of priors returned in the
\verb{$priors} element of each model module (see \code{\link[=enw_reference]{enw_reference()}},
\code{\link[=enw_report]{enw_report()}}, \code{\link[=enw_expectation]{enw_expectation()}}, \code{\link[=enw_missing]{enw_missing()}}, and \code{\link[=enw_obs]{enw_obs()}}).
Each prior is a \verb{<dist_spec>} object from the \code{distspec} package (for
example \code{\link[distspec:Normal]{distspec::Normal()}} or \code{\link[distspec:LogNormal]{distspec::LogNormal()}}) holding the
location and scale that the Stan model applies.
}
\keyword{internal}
