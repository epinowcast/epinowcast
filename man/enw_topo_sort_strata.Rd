% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strata.R
\name{enw_topo_sort_strata}
\alias{enw_topo_sort_strata}
\title{Topologically sort a multi-stratum dependency graph}
\usage{
enw_topo_sort_strata(strata_spec)
}
\arguments{
\item{strata_spec}{A named list keyed by stratum name. Each element
describes one stratum and must contain a logical \code{dependent} flag and,
when \code{dependent} is \code{TRUE}, a \code{parent} string naming the stratum it
depends on. The structure produced by \code{\link[=enw_expectation]{enw_expectation()}} for a named
list \code{r} satisfies this; the \code{secondary} element (if present) is also
accepted as a source of the parent.}
}
\value{
A list with:
\itemize{
\item \code{order}: a character vector of stratum names in topological order
(parents before dependents).
\item \code{parent}: a named character vector mapping each stratum to its
parent, with \code{NA} for independent strata.
\item \code{dependent}: a named logical vector flagging dependent strata.
}
}
\description{
Builds the dependency graph implied by the per-stratum
expectation specification and returns a topological ordering so that a
parent stratum is always evaluated before any stratum that depends on
it. Independent strata (those with their own latent process) have no
parent; dependent strata (those declared with \code{\link[=secondary]{secondary()}}) name a
single parent. Chains (\code{cases -> hosp -> deaths}) and shared parents
(a diamond) are allowed; cycles, self-dependencies, and references to
an unknown parent are errors.

This is part of the Phase 0 R skeleton of the multi-stratum
expectation overlay (see the package issue tracker); it validates and
orders the graph but does not yet emit Stan data.
}
\examples{
spec <- list(
  cases = list(dependent = FALSE),
  hosp = list(dependent = TRUE, parent = "cases"),
  deaths = list(dependent = TRUE, parent = "hosp")
)
enw_topo_sort_strata(spec)
}
\seealso{
Other strata: 
\code{\link{.resolve_stratum_spec}()}
}
\concept{strata}
