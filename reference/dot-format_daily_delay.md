# Format delay for daily timestep

Format delay for daily timestep

## Usage

``` r
.format_daily_delay(daily_max_delay)
```

## Arguments

- daily_max_delay:

  Delay value in days

## Value

Formatted string
